% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/melsi_robust.R
\name{generate_test_data}
\alias{generate_test_data}
\title{Generate Synthetic Microbiome Test Data}
\usage{
generate_test_data(n_samples = 40, n_taxa = 100, n_signal_taxa = 8)
}
\arguments{
\item{n_samples}{Number of samples to generate (default: 40)}

\item{n_taxa}{Number of taxa to generate (default: 100)}

\item{n_signal_taxa}{Number of taxa with treatment signal (default: 8)}
}
\value{
A list containing:
\item{counts}{Matrix of synthetic microbiome counts with samples as rows and taxa as columns}
\item{metadata}{Data frame with sample metadata including group assignments}
}
\description{
Generates synthetic microbiome count data with known group differences for testing
and demonstration purposes. The data includes realistic abundance distributions
and treatment effects.
}
\details{
The function generates count data using negative binomial distributions to simulate
realistic microbiome abundance patterns. Treatment groups show differential abundance
in a subset of taxa, with some taxa increasing and others decreasing in the treatment group.

The synthetic data is useful for:
\itemize{
\item Testing MeLSI functionality
\item Benchmarking performance
\item Demonstrating usage
\item Validation studies
}
}
\examples{
# Generate test data
test_data <- generate_test_data(n_samples = 60, n_taxa = 150, n_signal_taxa = 10)

# Extract counts and metadata
X <- test_data$counts
metadata <- test_data$metadata

# Display data dimensions
cat("Samples:", nrow(X), "\n")
cat("Taxa:", ncol(X), "\n")
cat("Groups:", paste(unique(metadata$Group), collapse = ", "), "\n")
}
\seealso{
\code{\link{run_melsi_permutation_test}}, \code{\link{test_improved_melsi}}
}
